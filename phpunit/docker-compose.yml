version: "3.9"
services:
  phpunit-on-php81:
    image: php:8.1.5-cli
    entrypoint: vendor/bin/phpunit
    working_dir: /src
    volumes:
      - ".:/src"

  phpunit-on-php74:
    image: php:8.1.5-cli
    entrypoint: vendor/bin/phpunit
    working_dir: /src
    volumes:
      - ".:/src"
