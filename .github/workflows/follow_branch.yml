name: Follow branch

on:
  push:
    branches: master

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - run: git checkout -B default origin/master
    - run: git push origin default
