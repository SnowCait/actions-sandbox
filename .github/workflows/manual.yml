name: Manual Trigger

on:
  workflow_dispatch:
    inputs:
      ログレベル:
        required: true
        description: ログレベル
        default: warning
      タグ:

jobs:
  printInputs:
    runs-on: ubuntu-latest

    steps:
    - run: |
        echo "Log level: ${{ github.event.inputs.ログレベル }}"
        echo "Tags: ${{ github.event.inputs.タグ }}" 
