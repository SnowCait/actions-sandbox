name: Protected branch (server)

on:
  push:
    paths:
      - server/**
      - .github/workflows/protected-branch-server.yml

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - run: echo "success"
