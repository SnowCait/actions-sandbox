name: Status Trigger

on:
  status:
  check_run:
  check_suite:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - run: echo $GITHUB_EVENT_NAME
      - run: cat $GITHUB_EVENT_PATH
