name: Bug

on:
  push:
    paths:
      - .github/workflows/bug.yml

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - id: stepA
        run: echo "::set-output name=json::[1,2]"
      - run: echo $STR
        env:
          STR: ${{ join(fromJSON(steps.stepA.outputs.json)) }}
      - run: echo $STR
        env:
          STR: ${{ join(fromJSON(steps.stepA.outputs.json)) }}
        if: false
      - id: stepB
        run: echo "::set-output name=json::[1,2]"
        if: false
      - run: echo $STR
        env:
          STR: ${{ steps.stepB.outputs.json }}
        if: false
      - run: echo $STR
        env:
          STR: ${{ join(fromJSON(steps.stepB.outputs.json)) }}
        if: false
