name: Textlint

on:
  pull_request:

jobs:
  textlint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: SnowCait/action-textlint@up-to-npx
        with:
          fail_on_error: true
